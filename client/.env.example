# Copy to .env.local and adjust.

# FastAPI base URL. Leave unset in dev — vite.config.js proxies /api to :8000.
# VITE_API_URL=http://localhost:8000/api

# Set to false once the backend endpoints are live to stop using mock data.
VITE_USE_MOCKS=true
